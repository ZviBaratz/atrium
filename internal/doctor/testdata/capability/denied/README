Byte-for-byte rich, except that it DENIES a server it also configures. claude matches
a denial by serverName, so slack is configured here and unreachable, and the fixture
exists to hold availableMCPServers to that: the MCP axis must report linear alone. Read
as the configured set, this dir would be reported as a substitute for rich when it
cannot run one of rich's two servers.
