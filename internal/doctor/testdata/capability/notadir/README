A config dir claude was never onboarded in: it holds neither settings.json nor
.claude.json, so ReadDirCapabilities cannot answer for it. The file is here only
because git does not track empty directories.
